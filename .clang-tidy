Checks: >
  -*,
  clang-analyzer-*,
  bugprone-*,
  modernize-*,
  performance-*,
  readability-*,
  -modernize-use-trailing-return-type,
  -readability-magic-numbers

# Only analyze headers in your project tree.
HeaderFilterRegex: '^(.*[/\\])?(src|include)[/\\].*'

WarningsAsErrors: ''
