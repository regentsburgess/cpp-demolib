@PACKAGE_INIT@

# If the exported library target gains a public dependency, make that dependency
# available before loading the targets file. Add one find_dependency() call per
# public dependency; private implementation dependencies do not belong here.
#
# include(CMakeFindDependencyMacro)
# find_dependency(SomePackage 1.2 CONFIG)

include("${CMAKE_CURRENT_LIST_DIR}/@LIB_PROJECT_NAME@Targets.cmake")

check_required_components(@LIB_PROJECT_NAME@)
